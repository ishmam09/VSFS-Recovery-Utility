# VSFS-Recovery-Utility
A fast, low-level file system consistency checker (fsck) written in C that automates metadata repair, bitmap reconstruction, and multi-level indirect block validation for a simulated Very Simple File System (VSFS).
